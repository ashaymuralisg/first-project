# Copy to .env and fill in. Never commit .env.
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# Staff auth — generate with: npm run hash -- 'your-strong-password'
STAFF_PASSWORD_HASH=
# Secret used to sign session cookies (use a long random string)
SESSION_SECRET=

# Where booking data lives
DB_PATH=./data/overbrod.db
# PDPA: days after the RESERVATION DATE (not the day it was booked) a
# booking is kept before automatic deletion.
RETENTION_DAYS=7

# Where uploaded media (images/video from the staff Media Library) is stored
# UPLOADS_DIR=./data/uploads

# Email (SMTP). If unset, emails are logged, not sent.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=OVERBRØD <overbrodsg@gmail.com>
DELI_EMAIL=overbrodsg@gmail.com

# Path to the static site to serve (defaults to ../site)
# SITE_DIR=../site
